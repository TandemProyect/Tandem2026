@model Desing.Models.ResetPasswordViewModel
@{
    Layout = "~/Views/Shared/_LayoutMaterioBlank.cshtml";
    ViewBag.Title = "Restablecer contrasena - " + (ViewBag.PlantillaBrandText as string ?? "T Desing.net");
}
<div class="position-relative">
    <div class="authentication-wrapper authentication-basic container-p-y">
        <div class="authentication-inner tandem-auth-inner py-6 mx-4">

            <div class="card p-sm-7 p-2">
                <div class="app-brand justify-content-center mt-5">
                    <a href="@Url.Action("Login", "Account")" class="app-brand-link gap-3">
                        <span class="app-brand-logo demo">
                            <img src="~/Content/images/Login/at.png" alt="Tandem" style="height:36px" />
                        </span>
                        <span class="app-brand-text demo text-heading brand-svg-login">
                            @Html.Partial("_BrandSvg")
                        </span>
                    </a>
                </div>

                <div class="card-body mt-1">
                    <h4 class="mb-1">Restablecer contrasena</h4>
                    <p class="mb-5">Introduce tu nueva contrasena para volver a acceder a @(ViewBag.PlantillaBrandText ?? "T Desing.net").</p>

                    @using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post, new { id = "formReset", @class = "mb-5", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.Code)
                        @Html.ValidationSummary(true, "", new { @class = "text-danger small d-block mb-3" })

                        <div class="form-floating form-floating-outline mb-5 form-control-validation">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", id = "email", placeholder = "Email" })
                            <label for="email">Email</label>
                        </div>
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger small d-block mb-3" })

                        <div class="form-floating form-floating-outline mb-5 form-control-validation">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control", id = "password", placeholder = "Nueva contrasena" })
                            <label for="password">Nueva contrasena</label>
                        </div>
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger small d-block mb-3" })

                        <div class="form-floating form-floating-outline mb-5 form-control-validation">
                            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", id = "confirmPassword", placeholder = "Confirmar contrasena" })
                            <label for="confirmPassword">Confirmar contrasena</label>
                        </div>
                        @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger small d-block mb-3" })

                        <div class="mb-5">
                            <button class="btn btn-primary d-grid w-100" type="submit">Cambiar contrasena</button>
                        </div>

                        <div class="text-center">
                            <a href="@Url.Action("Login", "Account")" class="d-flex align-items-center justify-content-center">
                                <i class="icon-base ri ri-arrow-left-s-line me-1"></i>
                                <span>Volver a iniciar sesion</span>
                            </a>
                        </div>
                    }
                </div>
            </div>

            <img src="~/assets/img/login-construction-skyline.png" class="tandem-login-skyline d-none d-lg-block" alt="construction-skyline" />
        </div>
    </div>
</div>

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
